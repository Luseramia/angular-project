import { Component, Input, inject } from '@angular/core';
import { ActivatedRoute } from '@angular/router';
@Component({
  selector: 'loading',
  standalone: true,
  imports: [],
  templateUrl: '../components-html/loading.html',
})
export class Loading {
}
